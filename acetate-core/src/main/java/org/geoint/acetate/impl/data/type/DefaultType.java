package org.geoint.acetate.impl.data.type;

/**
 * Identifies the {@link DataType} as the default implementation, which is used
 * unless the application defines a specific DataType implementation to use.
 */
public @interface DefaultType {

}
