/**
 * Data binding programmatic interface.
 *
 * It's recommended that application uses the high-level programmatic API
 * ({@link Acetate}) or a framework library such as a JAX-RS extension, if
 * available. While this package is version controlled, due to its relative
 * low-level nature (comparatively) it likely will be the most volatile.
 */
package org.geoint.acetate.impl.bind;
