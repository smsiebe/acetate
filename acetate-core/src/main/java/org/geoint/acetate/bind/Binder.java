package org.geoint.acetate.bind;

import org.geoint.concurrent.ThreadSafe;

/**
 * 
 */
@ThreadSafe
public final class Binder {

    public static <T> BindOptions<T> source (DataModel model, DataBinder<T> binder) {
        
    }
}
